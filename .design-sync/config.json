{
  "pkg": "megustastu-bookings",
  "globalName": "MGTBookings",
  "shape": "package",
  "buildCmd": "npx vite build --config .design-sync/vite.lib.config.mjs",
  "entry": "./.design-sync/.lib/index.js",
  "cssEntry": ".design-sync/.lib/index.css",
  "componentSrcMap": {
    "AlertIcon": "src/components/Icons.jsx",
    "ArchiveIcon": "src/components/whatsapp/WaIcons.jsx",
    "AssignIcon": "src/components/Icons.jsx",
    "AutoHeight": "src/components/atoms.jsx",
    "BellIcon": "src/components/Icons.jsx",
    "BellRingIcon": "src/components/Icons.jsx",
    "BookingFormModal": "src/components/BookingFormModal.jsx",
    "ChairIcon": "src/components/Icons.jsx",
    "CheckIcon": "src/components/Icons.jsx",
    "ChevronDownIcon": "src/components/Icons.jsx",
    "ChevronLeftIcon": "src/components/Icons.jsx",
    "ChevronRightIcon": "src/components/Icons.jsx",
    "ChevronUpIcon": "src/components/Icons.jsx",
    "ClashIcon": "src/components/Icons.jsx",
    "CloseIcon": "src/components/Icons.jsx",
    "ClosedIcon": "src/components/Icons.jsx",
    "CogIcon": "src/components/Icons.jsx",
    "Collapsible": "src/components/atoms.jsx",
    "CopyIcon": "src/components/Icons.jsx",
    "DateField": "src/components/atoms.jsx",
    "DepositIcon": "src/components/Icons.jsx",
    "DiceIcon": null,
    "DoorGlyph": "src/components/FloorGlyphs.jsx",
    "DoubleCheckIcon": "src/components/Icons.jsx",
    "DownloadIcon": "src/components/Icons.jsx",
    "DraftIcon": "src/components/whatsapp/WaIcons.jsx",
    "EditIcon": "src/components/Icons.jsx",
    "FlaskIcon": null,
    "Fld": "src/components/atoms.jsx",
    "HistoryIcon": "src/components/Icons.jsx",
    "InlineAlert": "src/components/atoms.jsx",
    "Kbd": "src/components/atoms.jsx",
    "LateIcon": "src/components/Icons.jsx",
    "LinkIcon": "src/components/whatsapp/WaIcons.jsx",
    "ListView": "src/components/ListView.jsx",
    "LockIcon": "src/components/Icons.jsx",
    "MessageBubble": "src/components/whatsapp/MessageBubble.jsx",
    "ModalPresence": "src/components/atoms.jsx",
    "ModalTitle": "src/components/atoms.jsx",
    "NoShowIcon": "src/components/Icons.jsx",
    "OfflineIcon": "src/components/Icons.jsx",
    "OutlineChip": "src/components/atoms.jsx",
    "OverlapIcon": "src/components/Icons.jsx",
    "Overlay": "src/components/atoms.jsx",
    "PencilIcon": "src/components/whatsapp/WaIcons.jsx",
    "PlanView": "src/components/PlanView.jsx",
    "Presence": "src/components/atoms.jsx",
    "PrintIcon": "src/components/Icons.jsx",
    "RecheckIcon": "src/components/whatsapp/WaIcons.jsx",
    "RestoreIcon": "src/components/whatsapp/WaIcons.jsx",
    "RetryIcon": "src/components/whatsapp/WaIcons.jsx",
    "Reveal": "src/components/atoms.jsx",
    "SBadge": "src/components/atoms.jsx",
    "SearchField": "src/components/atoms.jsx",
    "SearchIcon": "src/components/Icons.jsx",
    "Section": "src/components/atoms.jsx",
    "SelectIcon": "src/components/whatsapp/WaIcons.jsx",
    "SizeRing": "src/components/atoms.jsx",
    "SlideView": "src/components/atoms.jsx",
    "SmallTag": "src/components/atoms.jsx",
    "SplitSideIcon": "src/components/Icons.jsx",
    "SplitStackIcon": "src/components/Icons.jsx",
    "StarIcon": "src/components/Icons.jsx",
    "StatusIcon": "src/components/Icons.jsx",
    "SwapIcon": "src/components/Icons.jsx",
    "TBadge": "src/components/atoms.jsx",
    "TableGlyph": "src/components/FloorGlyphs.jsx",
    "TableGrid": "src/components/TableGrid.jsx",
    "TemplatesIcon": "src/components/whatsapp/WaIcons.jsx",
    "TimelineView": "src/components/TimelineView.jsx",
    "Toast": "src/components/atoms.jsx",
    "Toggle": "src/components/atoms.jsx",
    "TrashIcon": "src/components/whatsapp/WaIcons.jsx",
    "VoucherIcon": "src/components/Icons.jsx",
    "WaitIcon": "src/components/Icons.jsx",
    "WarnIcon": "src/components/whatsapp/WaIcons.jsx",
    "WeekView": "src/components/WeekView.jsx"
  },
  "docsDir": ".design-sync/docs",
  "dtsPropsFor": {
    "AlertIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ArchiveIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "AssignIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "AutoHeight": "  children?: React.ReactNode;\n  /** An identity (e.g. the active tab) to re-measure on synchronously when the whole content is swapped. */\n  watch?: unknown;\n  style?: React.CSSProperties;",
    "BellIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "BellRingIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ChairIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "CheckIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ChevronDownIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ChevronLeftIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ChevronRightIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ChevronUpIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ClashIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "CloseIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ClosedIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "CogIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "Collapsible": "  /** The header text. */\n  title: React.ReactNode;\n  /** Shown under the title while OPEN. */\n  subtitle?: React.ReactNode;\n  /** Shown on the right while COLLAPSED — a one-line summary of the contents. */\n  summary?: React.ReactNode;\n  /** Uncontrolled initial state. Default false. */\n  defaultOpen?: boolean;\n  /** Controlled mode: pass open + onToggle. */\n  open?: boolean;\n  onToggle?: (next: boolean) => void;\n  /** The body, eased open/closed. */\n  children?: React.ReactNode;\n  /** Merged over the Section's style. */\n  style?: React.CSSProperties;",
    "CopyIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "DateField": "  /** ISO date, \"YYYY-MM-DD\" (\"\" shows an empty field and no weekday). */\n  value: string;\n  /** The native input's change event — read e.target.value. */\n  onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;\n  /** The PILL's look (lands on the wrapper). Pass mkInp() in a form. */\n  style?: React.CSSProperties;\n  /** Attributes for the <input> itself: id, aria-label, Fld's attrs, readOnly. */\n  inputProps?: React.InputHTMLAttributes<HTMLInputElement>;",
    "DepositIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "DoorGlyph": "  /** Door record, centimetres: centre x/y, width, rotation in degrees, flip = hinge on the other side. */\n  door: { x: number; y: number; width: number; rot?: number; flip?: boolean };\n  /** Draw in the accent colour. */\n  selected?: boolean;\n  onPointerDown?: (e: React.PointerEvent) => void;\n  onClick?: (e: React.MouseEvent) => void;",
    "DoubleCheckIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "DownloadIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "DraftIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "EditIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "Fld": "  /** The visible label. */\n  label: React.ReactNode;\n  /** Required field: adds a red * and aria-required on the control. */\n  req?: boolean;\n  /** Mark the field invalid (only while its error message is on screen). */\n  invalid?: boolean;\n  /** id of the rendered error message, announced with the field when invalid. */\n  describedBy?: string;\n  /** Merged over the field's column style. */\n  style?: React.CSSProperties;\n  /** ONE control: a function receiving (id, attrs) — put id on the control and spread attrs. SEVERAL controls: plain elements (the field becomes a named role=\"group\"). */\n  children: ((id: string, attrs: Record<string, string> | null) => React.ReactNode) | React.ReactNode;",
    "HistoryIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "InlineAlert": "  /** Text/mark colour. Take it from ALERT_TONES with its tint — never pick one alone. Default ALERT_TONES.danger.tone. */\n  tone?: string;\n  /** Pane fill. Default ALERT_TONES.danger.tint. */\n  tint?: string;\n  /** Leading mark: any icon component. Default AlertIcon; null renders text only. */\n  icon?: React.ComponentType<{ size?: number }> | null;\n  /** id for aria-describedby from the field it is about. */\n  id?: string;\n  /** Merged over the pane's style. */\n  style?: React.CSSProperties;\n  /** The message. */\n  children?: React.ReactNode;",
    "Kbd": "  /** The key's label, e.g. \"N\", \"⌘\", \"Esc\". */\n  k: React.ReactNode;",
    "LateIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "LinkIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "LockIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "ModalPresence": "  /** Whether the modal is open. On false the modal stays mounted until its exit animation ends. */\n  show: boolean;\n  /** The modal — typically `show ? <Overlay …/> : null`. */\n  children?: React.ReactNode;\n  /** How long to keep it mounted after close, ms. Default EXIT_MS (derived from the motion tokens). */\n  outMs?: number;",
    "ModalTitle": "  /** REQUIRED — the colour of the button that opened the modal (create/act), or var(--app-btn-grey-strong) for configure/read surfaces. */\n  background: string;\n  /** Space below the pill, px. Default 14. */\n  marginBottom?: number;\n  /** The title text. */\n  children?: React.ReactNode;",
    "NoShowIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "OfflineIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "OutlineChip": "  /** Semantic tone (CHIP_TONES). Default \"neutral\". */\n  tone?: \"success\" | \"warn\" | \"danger\" | \"neutral\";\n  /** Element to render. \"button\" for a clickable chip. Default \"span\". */\n  as?: \"span\" | \"button\" | \"div\";\n  /** \"micro\" (T.micro, default) or \"small\" (T.small). */\n  size?: \"micro\" | \"small\";\n  style?: React.CSSProperties;\n  children?: React.ReactNode;\n  /** Any other attribute (onClick, title, aria-*, className) is passed to the element. */\n  [attr: string]: unknown;",
    "OverlapIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "Overlay": "  /** Called when the scrim is clicked (desktop). Required — close the modal here. */\n  onClose: () => void;\n  /** The modal body. Start it with a <ModalTitle>. Scrolls when tall. */\n  children?: React.ReactNode;\n  /** Action row pinned to the bottom (Save / Cancel). Omit for read-only popovers. */\n  footer?: React.ReactNode;\n  /** Panel mode: a dialog that brings its own layout — no padding, no scroll port. */\n  panel?: { maxWidth?: number; height?: string | number; background?: string; blur?: number };",
    "PencilIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "Presence": "  /** Visible or not; on false it swaps to outClass, then unmounts after outMs. */\n  show: boolean;\n  /** Entrance class, e.g. \"mgt-slide-in\", \"mgt-toast-in\". */\n  inClass?: string;\n  /** Exit class, e.g. \"mgt-slide-out\", \"mgt-toast-out\". */\n  outClass?: string;\n  /** Unmount delay, ms. Default EXIT_MS. */\n  outMs?: number;\n  children?: React.ReactNode;\n  style?: React.CSSProperties;\n  /** Element to render. Default \"div\". */\n  tag?: string;",
    "PrintIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "RecheckIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "RestoreIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "RetryIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "Reveal": "  /** Open or closed. The content stays mounted until the collapse finishes. */\n  show: boolean;\n  children?: React.ReactNode;\n  style?: React.CSSProperties;\n  /** Ease WIDTH instead of height. */\n  horizontal?: boolean;\n  /** Motion step: \"reveal\" (a disclosure under your finger, default) or \"move\" (something arriving on its own). */\n  speed?: \"reveal\" | \"move\" | \"tap\" | \"shift\";\n  /** Remove the two wrapper divs from the accessibility tree (between a list and its items). */\n  presentational?: boolean;",
    "SBadge": "  /** The booking status. */\n  status: \"pending\" | \"confirmed\" | \"seated\" | \"completed\" | \"cancelled\";",
    "SearchField": "  /** The current query. */\n  value: string;\n  /** The input's change event — read e.target.value. */\n  onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;\n  /** Called by the ✕ button; clear your query here (focus returns to the input). */\n  onClear: () => void;\n  placeholder?: string;\n  /** Accessible name for the input, e.g. \"Search bookings\". */\n  ariaLabel?: string;\n  /** Merged over the pill's style (based on mkInp()). */\n  style?: React.CSSProperties;",
    "SearchIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "Section": "  /** Merged over the card's defaults (bg-soft fill, soft border, R.card, 14px padding, 14px bottom margin). */\n  style?: React.CSSProperties;\n  children?: React.ReactNode;",
    "SelectIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "SizeRing": "  /** Party size. */\n  n: number;\n  /** Rim colour. Default: translucent white for saturated block fills; use \"var(--chip-neutral-border)\" on light surfaces. */\n  rim?: string;\n  style?: React.CSSProperties;",
    "SlideView": "  /** The entrance CLASS: \"mgt-view-in-left\" | \"mgt-view-in-right\" (switching views) or \"mgt-view-fade\" (a date change). */\n  dir: string;\n  /** Pass a definite height through inside a fixed-height flex column. */\n  fill?: boolean;\n  children?: React.ReactNode;",
    "SmallTag": "  /** The tag's content — text, or an icon plus text. */\n  label: React.ReactNode;\n  /** REQUIRED in practice: SmallTag has no colours of its own — pass background and color. */\n  style?: React.CSSProperties;",
    "SplitSideIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;",
    "SplitStackIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;",
    "StarIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;",
    "StatusIcon": "  /** The booking status to mark. Anything else renders nothing. */\n  status: \"pending\" | \"confirmed\" | \"seated\" | \"completed\" | \"cancelled\";\n  /** px — use the IC scale. Default IC.chrome. */\n  size?: number;\n  stroke?: number;",
    "SwapIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "TBadge": "  /** Table id, e.g. \"5A\" (outdoor, teal) or \"i2\" (indoor, purple). */\n  id: string;",
    "TableGlyph": "  /** Table id — drives the label and its zone colour. */\n  id: string;\n  /** Floor-plan record, centimetres: centre x/y, rotation in degrees, shape, width (and height for \"rect\"), chairs per side. */\n  entry: { x: number; y: number; rot?: number; shape: \"round\" | \"rect\" | \"square\"; w: number; h?: number; chairs?: { top?: number; right?: number; bottom?: number; left?: number } };\n  /** Table fill — BLOCK_BG[status] when occupied, \"var(--bg-card)\" when free. */\n  fill?: string;\n  /** Outline — \"rgba(255,255,255,0.5)\" when occupied, \"var(--fp-outline)\" when free. */\n  stroke?: string;\n  strokeWidth?: number;\n  strokeDasharray?: string;\n  /** Appended to the id in the label pill. */\n  labelSuffix?: string;\n  /** Chair fill. Default \"var(--bg-stepper)\". */\n  chairFill?: string;\n  /** Accessible name when clickable, e.g. \"Table 3, Pau Estévez, 20:00, 4 guests, confirmed\". */\n  ariaLabel?: string;\n  /** Drawn at the table's un-rotated centre (e.g. a StatusIcon). */\n  children?: React.ReactNode;\n  /** Makes the table a focusable, keyboard-operable button. */\n  onClick?: (e: React.SyntheticEvent) => void;\n  onPointerDown?: (e: React.PointerEvent) => void;\n  onContextMenu?: (e: React.MouseEvent) => void;\n  style?: React.CSSProperties;\n  /** Style for the table shape itself (e.g. a fill transition). */\n  shapeStyle?: React.CSSProperties;",
    "TemplatesIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "Toast": "  /** Visible or not; on false it plays the exit, then unmounts. */\n  show: boolean;\n  /** The toast pane (the app's pane: var(--bg-ac-menu), 1px var(--border-card), R.card, var(--shadow-popover), leading status dot). */\n  children?: React.ReactNode;\n  /** Style on the animated wrapper (e.g. gridArea to crossfade toasts in one slot). */\n  style?: React.CSSProperties;",
    "Toggle": "  /** Whether the switch is on. */\n  on: boolean;\n  /** Flip it: onClick={() => setOn(!on)}. */\n  onClick: () => void;\n  /** REQUIRED — names what the switch controls, never its state. */\n  label: string;\n  /** Dims to 0.4 and removes it from the tab order. */\n  disabled?: boolean;",
    "TrashIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "VoucherIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "WaitIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke width override. Default: 2 at 18px and up, 2.2 below (optical compensation). */\n  stroke?: number;",
    "WarnIcon": "  /** px — use the IC scale (IC.inline 12 · IC.control 14 · IC.chrome 18). Default IC.chrome. */\n  size?: number;\n  /** Stroke colour. Default \"currentColor\" — prefer colouring the parent. */\n  color?: string;",
    "TimelineView": "  /** Every booking the app holds, all dates — the view filters to `date` and reads past visits itself. Shape: { id, name, phone, date: \"YYYY-MM-DD\", time: \"HH:MM\", size, duration (min), status: \"pending\" | \"confirmed\" | \"seated\" | \"completed\" | \"cancelled\", tables: string[], notes, deposit, preferredTables, _locked, _manual, noShow, stayedMin, updatedAt }. */\n  bookings: Array<Record<string, any>>;\n  /** The day shown, \"YYYY-MM-DD\". */\n  date: string;\n  /** Today's date, \"YYYY-MM-DD\" — the now-line and live markers appear only when date === today. */\n  today?: string;\n  /** Minutes since midnight for \"now\" (20:10 = 1210). */\n  nowMins?: number;\n  /** Zoom factor, 1 = the whole day across the grid. Owned by the parent. */\n  zoom?: number;\n  setZoom?: (zoom: number) => void;\n  /** Follow mode: keep the now-line pinned near the left edge. Owned by the parent. */\n  followNow?: boolean;\n  setFollowNow?: (on: boolean) => void;\n  /** Remembers the horizontal scroll across remounts — pass a useRef(0). */\n  scrollPosRef?: { current: number };\n  /** The optimiser switch shown above the grid. */\n  autoOptimizer?: boolean;\n  setAutoOptimizer?: (on: boolean) => void;\n  /** Table blocks: { id, date, tableId, from: \"HH:MM\", to: \"HH:MM\", allDay? } — drawn as red stripes. */\n  blocks?: Array<Record<string, any>>;\n  /** Running-late state by booking id. */\n  late?: Record<string, \"warn\" | \"noshow\">;\n  /** Minutes until a seated party's table is free, by booking id. */\n  freeing?: Record<string, number>;\n  /** Overstay warnings by booking id: { next, nextTime, gap, overdue }. */\n  warnings?: Record<string, { next: string; nextTime: string; gap: number; overdue: boolean }>;\n  /** Show the empty-day prompt (with onNew / emptyWalkin as its buttons). */\n  isEmpty?: boolean;\n  /** Tap a block. */\n  onEdit?: (booking: Record<string, any>) => void;\n  /** The block's assign handle. */\n  onManual?: (bookingId: string) => void;\n  /** Quick status change (hold / right-click). */\n  onStatus?: (bookingId: string, status: string) => void;\n  /** Tap a table label to block it. */\n  onBlock?: (tableId: string) => void;\n  onNoShow?: (bookingId: string) => void;\n  onNew?: (() => void) | null;\n  emptyWalkin?: (() => void) | null;\n  /** Deposit currency marker. Default \"€\". */\n  currency?: string;",
    "ListView": "  /** Every booking the app holds, all dates — the view filters to `date` and reads past visits itself. Shape: { id, name, phone, date: \"YYYY-MM-DD\", time: \"HH:MM\", size, duration (min), status: \"pending\" | \"confirmed\" | \"seated\" | \"completed\" | \"cancelled\", tables: string[], notes, deposit, preferredTables, _locked, _manual, noShow, stayedMin, updatedAt }. */\n  bookings: Array<Record<string, any>>;\n  /** The day shown, \"YYYY-MM-DD\". */\n  date: string;\n  today?: string;\n  /** Minutes since midnight for \"now\". */\n  nowMins?: number;\n  /** Overstay warnings by booking id: { next, nextTime, gap, overdue }. */\n  warnings?: Record<string, { next: string; nextTime: string; gap: number; overdue: boolean }>;\n  /** Running-late state by booking id; \"noshow\" adds a one-tap No show. */\n  late?: Record<string, \"warn\" | \"noshow\">;\n  /** The selected card (accent rim). */\n  selectedId?: string | null;\n  onSelect?: (bookingId: string) => void;\n  /** Bump to scroll the selected card into view (the parent's keyboard navigation). */\n  focusReq?: number;\n  /** Whether the \"Completed & cancelled\" fold is open. Owned by the parent. */\n  showFinished?: boolean;\n  onToggleFinished?: () => void;\n  isEmpty?: boolean;\n  dayClosed?: boolean;\n  onEdit?: (booking: Record<string, any>) => void;\n  onStatus?: (bookingId: string, status: string) => void;\n  onManual?: (bookingId: string) => void;\n  onDelete?: (bookingId: string) => void;\n  onNoShow?: (bookingId: string) => void;\n  onNew?: (() => void) | null;\n  emptyWalkin?: (() => void) | null;\n  currency?: string;",
    "PlanView": "  /** Every booking the app holds, all dates — the view filters to `date` and reads past visits itself. Shape: { id, name, phone, date: \"YYYY-MM-DD\", time: \"HH:MM\", size, duration (min), status: \"pending\" | \"confirmed\" | \"seated\" | \"completed\" | \"cancelled\", tables: string[], notes, deposit, preferredTables, _locked, _manual, noShow, stayedMin, updatedAt }. */\n  bookings: Array<Record<string, any>>;\n  /** The day shown, \"YYYY-MM-DD\". \"Today\" is read from the device clock. */\n  date: string;\n  /** { ...DEFAULT_LAYOUT, floorPlan } — tables (ids, zones, capacities) plus the floor plan: { room: { w, h }, walls: [{ x1, y1, x2, y2 }], doors: [{ x, y, width, rot, flip }], tables: { [id]: { x, y, shape: \"square\" | \"rect\" | \"round\", w, h, rot, chairs: { top, right, bottom, left } } } }, in centimetres. */\n  layout: Record<string, any>;\n  /** Table blocks: { id, date, tableId, from, to, allDay? }. */\n  blocks?: Array<Record<string, any>>;\n  /** Minutes since midnight for \"now\" — the time strip starts here on today. */\n  nowMins?: number;\n  late?: Record<string, \"warn\" | \"noshow\">;\n  /** Minutes until a seated party's table is free, by booking id — the \"~Nm\" pill. */\n  freeing?: Record<string, number>;\n  /** Wheel/pinch zoom, drag pan, double-tap reset. Default true. */\n  gesturesEnabled?: boolean;\n  /** Turnaround minutes between parties (0 = off). */\n  turnBuffer?: number;\n  isEmpty?: boolean;\n  dayClosed?: boolean;\n  onEdit?: (booking: Record<string, any>) => void;\n  onStatus?: (bookingId: string, status: string) => void;\n  onNoShow?: (bookingId: string) => void;\n  /** \"Walk-in here\" on a free table. */\n  onWalkin?: (tableId: string) => void;\n  onNew?: (() => void) | null;\n  emptyWalkin?: (() => void) | null;",
    "WeekView": "  /** Every booking, all dates — cover and booking counts per day (cancelled excluded). */\n  bookings: Array<Record<string, any>>;\n  /** The date the calendar opens on (\"YYYY-MM-DD\"); it shows that week or month. */\n  viewDate: string;\n  /** A day was picked (tap, or Enter on the focused day). */\n  onPick: (date: string) => void;\n  /** Close (Close button or scrim). */\n  onClose: () => void;",
    "BookingFormModal": "  /** The draft, owned by the parent. Start from EMPTY_FORM: { name, phone, date, time: \"HH:MM\", size, preference: \"auto\" | \"outdoor\" | \"indoor\", notes, status, customDur, deposit, voucherCode, repeatWeekly, manualTables, preferredTables, returnOf, guestId, guestSeed }. */\n  form: Record<string, any>;\n  setForm: (next: Record<string, any> | ((prev: Record<string, any>) => Record<string, any>)) => void;\n  /** The booking being edited, or null for a new booking. */\n  editId: string | null;\n  /** A refused save's message, shown in an alert above the footer buttons (\"\" for none). */\n  error: string;\n  /** The field the error is about (\"name\", \"date\", \"time\" …) — for its aria wiring. */\n  errorField: string | null;\n  /** Every booking — autocomplete, past-visit chips, availability. */\n  bookings: Array<Record<string, any>>;\n  liveBookings: Array<Record<string, any>>;\n  /** Table blocks, for availability. */\n  tableBlocks: Array<Record<string, any>>;\n  /** Whether saving re-plans the whole day. */\n  autoOptimizer: boolean;\n  isMobile: boolean;\n  /** Offer \"Repeat weekly\". */\n  standingEnabled?: boolean;\n  /** Today's date and minutes since midnight — whether seating is still open on the draft's date. */\n  today?: string;\n  nowMins?: number;\n  currency?: string;\n  /** Completed visits before the \"Regular\" chip. Default 2. */\n  regularMin?: number;\n  vouchers?: Array<Record<string, any>>;\n  vouchersByCode?: Record<string, any>;\n  vouchersOn?: boolean;\n  onSave: () => void;\n  onSavePending?: () => void;\n  onSaveConfirm?: () => void;\n  onClose: () => void;\n  onClearSwap?: () => void;\n  /** \"Book again\": a new form pre-filled from this booking. */\n  onBookAgain?: (booking: Record<string, any>) => void;\n  onOpenPrefPicker?: () => void;\n  /** Open manual table assignment — the booking id, or \"__new__\" for the draft. */\n  onOpenManualAssign?: (target: string) => void;\n  onOpenHistory?: () => void;\n  onRequestCancel?: (bookingId: string) => void;\n  onRequestDelete?: (bookingId: string) => void;\n  onAddToWaitlist?: () => void;",
    "TableGrid": "  /** The picked table ids. */\n  selected: string[];\n  /** A cell was tapped — busy ones too, so ignore those here. */\n  toggle: (tableId: string) => void;\n  /** Tables taken in the window being assigned. */\n  busy: Set<string>;\n  /** Of those, the ones held by a seated party (never swappable). */\n  seatedBusy?: Set<string>;\n  /** Swap mode: a busy table whose party is not seated shows as \"swap\" and can be picked. */\n  swapBusy?: boolean;",
    "MessageBubble": "  /** One message. `ts` is epoch ms; `status` applies to outgoing messages; `isAutoAck` marks the automatic acknowledgement. */\n  msg: { id: string; direction: \"in\" | \"out\"; text: string; ts: number; status?: \"sending\" | \"delivered\" | \"read\" | \"failed\"; isAutoAck?: boolean };\n  /** The newest message in the thread — it rises in. */\n  isLast?: boolean;\n  /** Offer Retry on a failed outgoing message. */\n  onRetry?: (messageId: string) => void;"
  },
  "overrides": {
    "Overlay": {
      "cardMode": "single",
      "primaryStory": "FormWithFooter",
      "viewport": "900x740"
    },
    "ModalPresence": {
      "cardMode": "single",
      "viewport": "900x540"
    },
    "SBadge": {
      "cardMode": "column"
    },
    "SearchField": {
      "cardMode": "column"
    },
    "TimelineView": {
      "cardMode": "column",
      "primaryStory": "EveningService",
      "viewport": "900x1000"
    },
    "ListView": {
      "cardMode": "column",
      "primaryStory": "EveningService",
      "viewport": "900x1100"
    },
    "PlanView": {
      "cardMode": "column",
      "primaryStory": "EveningService",
      "viewport": "900x820"
    },
    "TableGrid": {
      "cardMode": "column",
      "primaryStory": "WalkIn"
    },
    "MessageBubble": {
      "cardMode": "column",
      "primaryStory": "Conversation"
    },
    "WeekView": {
      "cardMode": "single",
      "primaryStory": "Week",
      "viewport": "900x760"
    },
    "BookingFormModal": {
      "cardMode": "single",
      "primaryStory": "NewBooking",
      "viewport": "900x960"
    }
  },
  "extraFonts": [
    ".design-sync/fonts.css"
  ],
  "runtimeFontPrefixes": [
    "SF Pro"
  ],
  "readmeHeader": ".design-sync/conventions.md"
}
